{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Reclamation list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Objet</th>
                <th>Enonce</th>
                <th>Date</th>
                <th>Email</th>
                <th>Dest</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('reclamation_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.objet }}</td>
                <td>{{ entity.enonce }}</td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.dest }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('reclamation_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('reclamation_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('reclamation_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
